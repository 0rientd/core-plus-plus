# Core++

## How to compile
You only need:
- g++
- libsdl2-dev
- libsdl2-ttf-dev
- bear (optional, for linters)

For errors in linters but without errors in compilation, you can run `bear -- make` to generate a `compile_commands.json` file, which will help linters understand the project structure.
